<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- defers the rendering to the micropost partial (each element of @feed_items is a micropost) -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
